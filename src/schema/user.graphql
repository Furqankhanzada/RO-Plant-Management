# import Address from 'address.graphql'
# import Discount from 'discount.graphql'
# import Transaction from 'transaction.graphql'
# import Bottle from 'bottle.graphql'
scalar DateTime


type User {
  id: ID!
  name: String
  mobile: String!
  type: UserType
  role: Role!
  address: Address
  discounts: [Discount]
  transactions: [Transaction]
  bottle: Bottle
  createdAt: DateTime!
  updatedAt: DateTime!
}